# v7 jury story — 3 minutes

## 0:00–0:20 — The inversion
Most generative-media apps start with a prompt. CREATIVE//BOUNTY starts with money that already exists.

**Find money → check rights → authorize economics → generate → prove everything.**

## 0:20–0:45 — Counter-intuitive refusal
Open the real-opportunity radar.

Show a large advertised reward that is **REJECTED** because pursuing it violates the zero-user-capital constraint or requires an incompatible platform.

This demonstrates that advertised prize value is not confused with realizable economic value.

## 0:45–1:05 — Human review is a feature
Open an opportunity where AI use is allowed but contractual obligations remain.

Show **REVIEW**, not fake autonomous certainty.

## 1:05–1:45 — Qualified SAMPLE walkthrough
Run the deterministic SAMPLE opportunity.

Show:
1. rights PASS;
2. economic qualification;
3. budget authorization;
4. generation attempt #1 rejected;
5. attempt #2 accepted;
6. submission package generated.

Keep the SAMPLE badge visible.

## 1:45–2:15 — Trust layer
Open:
- Decision Certificate;
- audit receipt;
- replay endpoint.

Explain:
- certificate proves the decision payload was not altered;
- audit receipt proves evidence files were not altered;
- replay reconstructs and verifies the run without calling a provider.

## 2:15–2:40 — Genblaze + B2 LIVE proof
For the final submission, replace this section with one real run:
- Genblaze pipeline;
- provider fallback or retry configuration;
- durable B2 URL;
- `result.manifest.canonical_hash`;
- `result.manifest.verify() == True`.

Do not claim this until it exists.

## 2:40–3:00 — Close
**Most AI media apps generate first and search for value later. We reverse the order.**

CREATIVE//BOUNTY is an economic control plane for generative media:
**demand before generation, truth before claims.**
